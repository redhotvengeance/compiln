{
  "name": "compiln",
  "description": "Asset compiler middleware for Connect (node.js) that supports plugins.",
  "version": "0.1.0",
  "author": {
    "name": "Ian Lollar"
  },
  "keywords": ["build", "assets", "compile", "middleware", "plugins"],
  "engines" : ["node"],
  "main" : "./lib/compiln",
  "bin": {
    "compiln": "./bin/compiln"
  },
  "dependencies": {
    "node-fs": "0.1.4",
    "commander": "1.0.0"
  }
}
